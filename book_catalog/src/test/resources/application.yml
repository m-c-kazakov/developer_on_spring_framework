spring:
  datasource:
    username: test
    password: test
  sql:
    init:
      mode: always
      #schema-locations: schema.sql
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
    properties:
      hibernate:
      #format_sql: true
